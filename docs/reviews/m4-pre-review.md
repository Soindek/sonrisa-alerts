I reviewed the M4 diff (`main...HEAD`, 24 files, +693 / −363). The only file I changed is `prompts/log.md`, where I added this prompt's entry. Nothing is committed.

**Scope of the diff:** the backend adds the `/api` prefix and joins event and user details onto each delivery row. The frontend adds the admin page with Material, the dev proxy, `AlertsApi`, `InjectEventForm` and `DeliveryLogTable`, plus specs for those.

| # | Severity | file:line | Finding | Evidence | Suggested fix |
|---|---|---|---|---|---|
| 1 | minor | [apps/admin/src/app/delivery-log-table.ts:26](apps/admin/src/app/delivery-log-table.ts#L26) | Two table refreshes can overlap, and whichever response arrives last is shown, even if it's older. `refresh()` has two callers: the Refresh button and the form's `created` output. Disabling the button doesn't stop the second caller. If an inject finishes while a manual refresh is still running, the older result can overwrite the newer one. | `refresh(): void { this.loading.set(true); this.api.listDeliveries().subscribe({ next: (rows) => { this.rows.set(rows);` plus `app.html:6` `(created)="log.refresh()"` | Keep the current subscription and unsubscribe it before starting a new one, or feed refreshes through a `Subject` with `switchMap`. |
| 2 | minor | [apps/admin/src/app/inject-event-form.ts:36](apps/admin/src/app/inject-event-form.ts#L36) | The form checks severity's range but not that it's a whole number. The design says severity is "an integer 1–5", so `2.5` passes in the browser. The server rejects it (`create-event.dto.ts:19` `@IsInt()`) and the form shows the 400, so it's handled, just not up front. | `severity: [3, [Validators.required, Validators.min(1), Validators.max(5)]],` | Add `Validators.pattern(/^\d+$/)` and a matching `mat-error`, or leave it as is and say the server handles it. |
| 3 | minor | [apps/admin/src/app/delivery-log-table.ts:14](apps/admin/src/app/delivery-log-table.ts#L14) | The table has no spec. Nothing tests the per-status CSS class, the "unknown event" / "unknown user" text for null joins, the load-error message, or that Refresh re-fetches. The M4 prompt didn't ask for this spec, but this behaviour is new in the diff. | `export class DeliveryLogTable implements OnInit {` / `<span class="status" [class]="'status-' + row.status">` / `<span class="missing">unknown event</span>` | Add a small spec: flush rows with `event: null` and each status, then check the class names and text. Also flush an error and check `.load-error`. |
| 4 | minor | [apps/admin/src/app/inject-event-form.ts:56](apps/admin/src/app/inject-event-form.ts#L56) | The form's success path isn't tested. Nothing checks the "N deliveries" message or that `created` is emitted, and that output is what refreshes the table. The tags test does flush a success but asserts nothing afterwards (`inject-event-form.spec.ts:57`). The non-400 fallback (`Request failed (…)`) isn't tested either. | `this.createdCount.set(response.deliveries.length); this.created.emit(response.deliveries.length);` | After the success flush in the tags test, check the `.success` text and spy on `created`. Add one 500 case. |
| 5 | minor | [apps/admin/src/app/app.config.ts:9](apps/admin/src/app/app.config.ts#L9) | The router is dead code. `RouterOutlet` was removed from `App` and `routes` is `[]`, but `provideRouter` is still registered. | `provideRouter(routes),` with `app.routes.ts`: `export const routes: Routes = [];` | Remove `provideRouter` and `app.routes.ts`, or keep them and note that on purpose. |
| 6 | minor | [apps/admin/src/app/alerts-api.ts:34](apps/admin/src/app/alerts-api.ts#L34) | The response type is defined twice, once by hand in the admin app and once in the API, with no link between them. If the API changes, the admin types won't show a compile error. This is a normal trade-off without a shared library. | admin: `export interface DeliveryLogEntry extends Delivery {` · api `deliveries.controller.ts:8`: `export interface DeliveryLogEntry extends Delivery {` | Accept it and note it in the decision log, or add a shared types package later. Not worth doing in M4. |

**Clean categories:**
- **Correctness against the design:** no findings. The latest 100 rows come back newest first, the joins use two `findBy(In(...))` queries without relations (D23), a missing event or user becomes `null`, and the status values match `sent | dry-run | failed`. Matching, dedup and failure isolation aren't touched by this diff.
- **Error handling:** nothing beyond #1. Both `subscribe` calls handle errors, and if `list()` throws, Nest returns a 500.
- **Security:** no findings. No secrets and no new logging. Angular escapes the `error` and `title` text when it renders them. The unauthenticated `GET` returns user emails, but the M4 prompt asked for `user: { name, email }` and D06 says no auth.
- **Scope creep:** none. Everything in the diff was asked for in the M4 prompt.

**Note, not a finding:** the committed `docs/02-design.md` still says `POST /events` and `GET /deliveries`. The `/api` update is only in your uncommitted working tree, so make sure it goes into the M4 `docs:` commit.

**Verdict:** merge. All six findings are minor, and #1 is the only one that can show wrong data.
