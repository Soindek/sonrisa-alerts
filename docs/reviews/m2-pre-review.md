# Review of `1e3a854...HEAD` (the M2 vertical slice)

This range holds only the M2 commits (`57e2c3c`, `6ef5005`, merge `16d3168`). `feat/m3-slack-channel` has no commits of its own yet. The code diff is 28 files under `apps/api`, with 705 lines added and 95 removed. I read every changed source file in full. The `.env.example` file was blocked by a deny rule, so I didn't read it.

| # | Severity | file:line | Finding | Evidence | Suggested fix |
|---|---|---|---|---|---|
| 1 | major | [alert-pipeline.service.ts:48-51](apps/api/src/events/alert-pipeline.service.ts#L48-L51) | If saving a delivery row throws, the loop stops. Deliveries later in the plan are never tried, and the request returns 500 even though the event was already saved. The design only accepts that one send can go unrecorded. It still promises that a failure "never fails the request or the other deliveries". | `deliveries.push(await this.deliveries.save(this.deliveries.create({ eventId: event.id, ...plan, ...outcome })));` | Put a try/catch around each save, log the error and continue. Or state in 02-design / D22 that a failed row write also stops the rest of the loop. |
| 2 | major | [alert-pipeline.service.ts:56-71](apps/api/src/events/alert-pipeline.service.ts#L56-L71) | No test covers failure isolation, which is a central promise of the design (D22). Nothing tests the unknown channel, the missing user, a `send()` that throws, or that the other deliveries still happen after one fails. D26 dropped e2e tests only, not unit tests. | `} catch (err) { return { status: 'failed', error: err instanceof Error ? err.message : String(err) }; }` | Add a unit test for `AlertPipelineService` with fake repositories and a registry holding a throwing channel and a working channel. |
| 3 | minor | [config.ts:6](apps/api/src/config.ts#L6), [config.ts:25](apps/api/src/config.ts#L25) | The design says these defaults match docker-compose. They don't when a variable is set but empty: compose `${POSTGRES_PORT:-5432}` falls back to the default, but `??` doesn't, so `Number("")` becomes port `0`. `SMTP_HOST` already uses `\|\|` for exactly this reason. Unverified: whether `.env.example` ships these as empty values. | `port: Number(process.env.POSTGRES_PORT ?? 5432),` / `port: Number(process.env.SMTP_PORT ?? 587),` | Use `\|\|` for the two ports, like the host line does. |
| 4 | minor | [plan-deliveries.spec.ts:25-32](apps/api/src/matching/plan-deliveries.spec.ts#L25-L32) | The "first matching rule wins" test only covers the case where both rules match. There's no test where an earlier rule for the same user and channel does *not* match and a later one does. There's also no test for a rule with several channels. | `rule({ id: 'r1', userId: 'u1', channels: ['email'] }), rule({ id: 'r2', userId: 'u1', channels: ['email'] }),` | Add a test where `r1` fails on type and `r2` matches, expecting `ruleId: 'r2'`. Add one where a single rule has `['email','slack']`. |
| 5 | minor | [channel-registry.ts:10-12](apps/api/src/channels/channel-registry.ts#L10-L12), [email.channel.ts:24-41](apps/api/src/channels/email.channel.ts#L24-L41) | No test covers the registry throwing on a duplicate id at startup. Nothing tests the email subject/body format or the choice between dry-run and SMTP. Both are specified in the design. | `throw new Error(\`Duplicate notification channel id: ${channel.id}\`);` | Add small unit tests. `EmailChannel` already takes a `SmtpConfig` in its constructor, so it's easy to test. |
| 6 | minor | [alert-pipeline.service.ts:59](apps/api/src/events/alert-pipeline.service.ts#L59) | The error text differs from the design, which says `error "unknown channel"`. It only matters if a client or the admin view matches on that text. | ``return { status: 'failed', error: `Unknown channel: ${channelId}` };`` | Align either the code or the design text. |
| 7 | minor (unverified) | [alert-pipeline.service.ts:69](apps/api/src/events/alert-pipeline.service.ts#L69), [deliveries.controller.ts:12](apps/api/src/deliveries/deliveries.controller.ts#L12) | Raw error messages from nodemailer are stored and returned by `GET /deliveries`, which has no auth. SMTP errors can include the server's response text. I haven't confirmed they can contain credentials. | `error: err instanceof Error ? err.message : String(err)` | Accept it (no auth is a stated non-goal), or store a shortened message. |

**By category:**
- **Correctness:** matching, dedup order, status values and DTO rules match the design, apart from #1 and #6.
- **Error handling:** #1 is the only way a promise can reject without being handled. The seed and bootstrap run in a single process and use `await` throughout.
- **Security:** only #7. Logging the whole message in dry-run mode, including the recipient's email, is what the design specifies, so it isn't a finding.
- **Tests:** #2, #4, #5.
- **Scope creep:** none. Removing the e2e test and `vite-tsconfig-paths` is covered by D26 and D17.
- **Dead code, duplication, misleading names:** none found.

**Verdict:** fix first. #1 and #2 are worth fixing before M3 adds a second channel. The rest can be triaged.
