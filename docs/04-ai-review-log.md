# 04 — AI Review Log

What the AI produced, how I checked it, what I did with it. One row per meaningful output. Filled in during the build, not afterwards.

| # | Milestone | What was generated | Check applied | Verdict | Notes / what I changed |
|---|-----------|--------------------|---------------|---------|------------------------|
| 1 | M0 | UserPromptSubmit hook to log raw prompts | ran it (test prompt) | accepted with edits | Payload field name was a guess (payload.prompt) — verified by sending a test prompt and reading the file. It worked, but the IDE integration prepends an <ide_opened_file> context block that isn't part of what I typed; added a regex to strip it so the log stays a record of my own words. |
| 2 | M1 | Nest 12 / Angular 22 scaffolds, CLI version numbers | ran it | accepted | Versions treated as hallucination-prone; `npm view <pkg> version` and generated package.json confirmed them. |
| 3 | M1 | First explanation of the `npm install` crash (browser-playwright 5.0.1 vs vitest ^4) | read it | rejected | No package in the repo declares browser-playwright. Asked for evidence; CC withdrew the claim. |
| 4 | M1 | Second explanation + several scratch dry-runs + `overrides` proposal | ran it | accepted | Real install, `nest build`, `ng build`, `docker compose ps` healthy, `curl` 200 — "dry-run works" was not accepted as proof. Root cause marked unknown. |
| 5 | M1 | `docker-compose.yml` | read it | accepted with edits | Defaults added for all vars. -U was already present. Volume keeps its initial password — recreated with `compose down -v` after writing `.env`. |
| 6 | M1 | Scaffold tests after the vitest override | unit test | accepted | api 1/1 green, admin 2/2. Run now so M5 is not the first place to find a broken runner. |
| 7 | M1 | Staging for the M1 commit | read it | accepted with edits | CC flagged `tsconfig.build.tsbuildinfo` (build artifact) and `.claude/settings.json` (staged without being asked). Unstaged the first, kept the second, deleted CLI READMEs. |
| 8 | M1 | M1 commit | ran it | accepted with edits | CC flagged that my message did not match CLAUDE.md format; amended. |
| 9 | M1 | `prompts/log.md` timestamps reconciled with `raw.md` | read it | accepted | Two prompts have no raw entry; CC marked them instead of inventing timestamps. Cause not established: the hook appends every prompt it receives with no dedupe or filter beyond IDE blocks, so the script itself does not explain the gaps. |
| 10 | M1 | Row 9 text in this log, written without being asked | read it | accepted | CLAUDE.md reserves this file for me; CC edited it beyond the instruction. Content checked and kept. |
| 11 | M1 | Claim "process stopped and port 3000 is free" after the API smoke test | read it | rejected | Likely false: the dev server's task-notification (exit 1) arrived at 12:42:47Z, one second after I switched to main and the app files disappeared. The output file only says "exited with code 1", so the cause is inferred from timing, not confirmed. |
| 12 | M1 | UserPromptSubmit hook (prompt logging) | read it | accepted with edits | The hook also fires on background task notifications, so raw.md contains non-human entries (e.g. 12:42:47Z). Kept verbatim instead of filtered: a complete log beats a clean one. Rule for reading it: entries wrapped in <task-notification> are system events, not my prompts. |
| 13 | M2 | Dependency/peer check for the M2 packages | ran it | accepted with edits | Peer ranges confirmed. Skipping @types/nodemailer verified (nodemailer 10 ships `types`). Claim "typeorm 1.1.1 published today" was false (2026-09-01, npm view typeorm time); pinned 0.3.31 because CC was unsure of the 1.x API. |
| 14 | M2 | docs/02-design.md draft | read it | accepted with edits | Sequence said "load rules + their users" while Q2 said no relations — fixed to a separate user query. "First matching rule" by uuid order was arbitrary — added createdAt. Open questions resolved by me, D05 deviation kept and logged. |
| 15 | M2 | Vertical slice implementation (entities, `matchesRule`, `planDeliveries`, registry, email channel, pipeline, seed) | read it + unit test + ran it | accepted | Read the matcher, planner and pipeline against the spec; 19/19 tests green; live run: 201 with one `dry-run` row and the two-rule overlap collapsed to the first rule, 400 for `severity: 9`, `.env.example` in the commit checked for secrets. CC added unrequested but sensible details (SMTP port/secure defaults, "Recipient user not found" as `failed`, rules seeded one by one for `createdAt` order) — kept and logged in D23/D25. |
| 16 | M2 | CC's first sample curl and its explanation | read it | accepted with edits | From Git Bash the literal "Árvíz" reached Postgres as U+FFFD; the event still matched via its `flood` tag, so the first "success" proved nothing about accented keywords. CC caught this itself, re-sent with `\u` escapes and without the tag, and showed the UTF-8 bytes from Postgres. Explanation not independently verified. The garbled row stays in the dev DB until the next `compose down -v`. |
| 17 | M2 | CC's correction of the typeorm publish date | read it | accepted | CC explained it had read `time.modified` (last registry change) as the publish date. Plausible and consistent with `npm view typeorm time`. |
| 18 | M2 | `prompts/log.md` timestamps for M2 | compared with docs | rewritten by me | CC used its own clock (8–14 s later than `raw.md`) although M1 established raw.md as the source. Headers corrected to the raw.md values; CLAUDE.md now states the rule explicitly. |
| 19 | M2 | Unicode handling in `tokenize` | read it | accepted with edits | CC disclosed that there is no normalization, so a decomposed "á" does not match. Scheduled as an M3 fix (NFC) with a test. |
| 20 | M2 | AI pre-review of the M2 diff (`/review 1e3a854`, fresh session, D27) — report in `docs/reviews/m2-pre-review.md` | AI pre-review + read it | accepted with edits | 7 findings, each checked against the code. Accepted, fixed in M3: #1 a throwing row write stopped the loop and failed the request (contradicts D22); #2 no test for failure isolation; #3 `??` treats an empty env var as set (port 0) — switched to `\|\|` for all config values; #4 missing planner cases (earlier non-matching rule, multi-channel rule); #5 no registry/email-format tests; #6 design text says "unknown channel", code says "Unknown channel: <id>" — design aligned to code. Rejected: #7 raw SMTP errors in the unauthenticated delivery log — admin auth is a stated non-goal and SMTP replies carry status text, not credentials; accepted as a known risk. None of #1–#5 had been caught in my manual M2 read (row 15). |
| 21 | M3 | AI pre-review of the M3 diff (`/review main`, fresh session) — report in `docs/reviews/m3-pre-review.md` | AI pre-review + read it | accepted with edits | 6 findings, all accepted and fixed in a separate `fix:` commit: #1 tags were not NFC-normalized, so D20's "fixed in M3" was only half true; #2 event text reached Slack unescaped, so an unauthenticated `POST /events` with `<!channel>` could ping a whole channel; #3–#5 missing tests (fetch rejection/timeout, empty-env defaults, the missing-user error text); #6 design doc config section lacked `SLACK_WEBHOOK_URL`. Reviewer defect: several `file:line` references were diff positions, not file lines (e.g. `slack.channel.ts:227` in a 45-line file) — the command now requires HEAD line numbers. |
| 22 | M3 | Merged M3 build, run by me for the first time | manual test | accepted | `docker compose up -d` + `npm run dev:api`, one market event by curl: two `dry-run` rows for Bence (email + slack, rule R3), none for Anna (R1 is disaster-only, R2 needs severity 4); both dry-run texts match the spec; `GET /deliveries` also listed the two rows from Claude Code's earlier run, since the volume was kept and the seed correctly skipped. New observation no AI output had mentioned: a `pg` DeprecationWarning at startup (concurrent `client.query()`), traced with `--trace-deprecation` to TypeORM 0.3's schema synchronization (`RdbmsSchemaBuilder.build` → `getTables`, parallel queries on one connection), so a dev-only path — recorded under D24. |

Verdict values: `accepted` · `accepted with edits` · `rejected` · `rewritten by me`

Check values: `ran it` · `read it` · `unit test` · `manual test in UI` · `compared with docs`

## Patterns noticed
*(recurring failure modes of the AI on this task — filled in at the end)*
